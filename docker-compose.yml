services:
  deep-research:
    container_name: deep-research
    build: .
    env_file:
      - .env.local
    volumes:
      -  ./:/app/
    ports:
      - "3051:3051"
    tty: true
    stdin_open: true
    command: npm run api:docker
